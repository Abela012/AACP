import axios from 'axios';

async function test() {
    try {
        // Need to simulate a clerk token or bypass auth for testing...
        // Wait, I can just mock the DB call
    } catch (e) {
        console.error(e);
    }
}
test();
